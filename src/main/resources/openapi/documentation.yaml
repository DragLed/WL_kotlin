openapi: 3.0.0
info:
  title: Wishlist API
  version: 1.0.0

paths:
  /test:
    get:
      summary: Test endpoint
      responses:
        "200":
          description: OK

components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string